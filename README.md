# cdk
Experiments with CDK 
All CDK Experimens will be writen in Python 3.*

Key Commands
------------------------------------------------------------------------------
git log --oneline                               #Get Hash Tags of past commits
git log                                         #Get User Details too  
git config user.name
git config --global user.name "Your Name"
git config user.email
git config --global user.email "Your Email"

git status                                      #Of Current Repository
git init                                        #initialize new Repository 

git log --abbrev-commit 

git branch                                      #shows list of new branches 
git branch bugfix                               #makes new branch based on HEAD of current branch 
git switch branch-name                          #switch between branches : advancement of git checkout 
git switch -c branch-name                       #create new branch and change to new branch 


git commit -a -m "adding all files"             #add all files at one go with same comments 
